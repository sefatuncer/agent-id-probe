{
  "schema": "agent-id-probe/conformance-fixture/1",
  "id": "c12-violating-missing-resource-member",
  "modality": "oauth_metadata",
  "title": "Metadata document omits the required `resource` member",
  "exercises": {
    "C12": "violating"
  },
  "decision_rules": [
    "R3",
    "R9.1"
  ],
  "rationale": "The second C12 failure mode. Absence and mismatch are reported as different outcomes because they are different findings: one deployment never declared an identifier, the other declared the wrong one.",
  "spec": [
    {
      "source": "RFC 9728",
      "section": "3.3",
      "url": "https://www.rfc-editor.org/rfc/rfc9728.html#section-3.3",
      "quote": "The `resource` value returned MUST be identical to the protected resource's resource identifier value into which the well-known URI path suffix was inserted to create the URL used to retrieve the metadata. If these values are not identical, the data contained in the response MUST NOT be used.",
      "verification": "verbatim-verified in docs/spec-mapping.md"
    }
  ],
  "endpoint": {
    "url": "https://api.example.org/mcp",
    "response": {
      "status": 401,
      "headers": {}
    }
  },
  "documents": [
    {
      "url": "https://api.example.org/.well-known/oauth-protected-resource/mcp",
      "status": 200,
      "json": {
        "authorization_servers": [
          "https://auth.example.org"
        ]
      },
      "comment": "`authorization_servers` present, `resource` absent"
    },
    {
      "url": "https://auth.example.org/.well-known/oauth-authorization-server",
      "status": 200,
      "json": {
        "issuer": "https://auth.example.org",
        "code_challenge_methods_supported": [
          "S256"
        ]
      }
    }
  ],
  "expect": {
    "checks": {
      "C12": "fail_unimplemented",
      "C05": "pass",
      "C13": "pass"
    },
    "evidence": {
      "declared_resource": null
    }
  }
}
