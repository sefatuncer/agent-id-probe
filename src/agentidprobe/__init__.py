"""agent-id-probe: passive, spec-anchored measurement of identity and authorization
metadata published by deployed AI agent endpoints.

Design rule: every check maps to a normative sentence in a published specification.
The ground truth is the spec text, never the authors' judgement.
"""

__version__ = "0.1.0"
